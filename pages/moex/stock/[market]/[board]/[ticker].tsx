import { FC } from 'react';
import {moexService} from '@/api/moex/moex.service';
import {moexApi} from '@/api/moex/moex';

const MoexTicker: FC = ({ market, board }) => {

    return (
        <div>
            [ticker]
        </div>
    );
};

